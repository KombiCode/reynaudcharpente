<div class="max-w-3xl mx-auto mt-8">
  <div id="skills" class="flex flex-col items-baseline p-4 space-y-6 rounded shadow-lg">
    <% @skills.each do |skill| %>
      <%= render "skill", skill: skill %>
    <% end %>
  </div>
</div>