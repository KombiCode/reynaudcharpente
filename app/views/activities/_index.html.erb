<div id="activities" class="grid gap-8 p-8 sm:grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">
  <% if @activities.count %>
    <% @activities.order("id asc").each do |activity| %>
      <%= render "activities/activity", activity: activity %>
    <% end %>
  <% end %>
</div>
