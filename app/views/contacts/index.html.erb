<div class="max-w-3xl mx-auto mt-8">
  <div id="contacts" class="flex flex-col items-baseline p-4 space-y-6 rounded shadow-lg">
    <% @contacts.each do |contact| %>
      <%= render "contact", contact: contact %>
    <% end %>
  </div>
</div>